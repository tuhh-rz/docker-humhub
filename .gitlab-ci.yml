deployment_system:
    script:
        - docker build --no-cache -t local/humhub:$CI_BUILD_ID .
        - docker tag local/humhub:latest localhost:5000/humhub:latest
        - docker push localhost:5000/humhub:latest